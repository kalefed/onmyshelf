generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model Book {
  book_id     Int    @id @default(autoincrement())
  title       String
  author      String
  read_status String
  rating      Int?
}
